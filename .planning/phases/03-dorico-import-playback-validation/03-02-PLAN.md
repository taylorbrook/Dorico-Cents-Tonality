---
phase: 3
plan: 02
title: Tuner spot-check matrix + sparse/dense layout (HALion playback validation)
type: execute
wave: 2
depends_on: ["03-01"]
files_modified: []
autonomous: false
requirements: [PLAY-02, PLAY-03, UX-03]
must_haves:
  truths:
    - "All PLAY-03 named cases pass tuner spot-check at ±1¢ tolerance against HALion in Dorico Pro 6.x"
    - "Zero-deviation per base verified: `Sharp` plays +100¢ above natural; `Flat` plays -100¢ below natural; `Natural` plays at 0¢ (the literal note)"
    - "Off-by-100 trap diagnostic verified: `Sharp +50` plays +150¢ above natural; `Flat -7` plays -107¢ below natural"
    - "Boundaries verified: `Sharp +99` plays +199¢; `Sharp -99` plays +1¢; `Flat +99` plays -1¢; `Flat -99` plays -199¢; `Natural +99` plays +99¢; `Natural -99` plays -99¢"
    - "Enharmonic-equivalent pair verified: `Sharp -50` and `Natural +50` produce the same audible sounding pitch (both = +50¢ above natural reference)"
    - "PLAY-02 panel ↔ playback wiring closed implicitly — every PLAY-03 row exercises panel-application of an accidental and the resulting played pitch"
    - "Sparse-passage cent labels do not collide with note heads or ledger lines"
    - "Dense-passage `Sharp -50` + `Natural +50` + `Flat +50` chord stack: collision behavior documented in 03-VERIFICATION.md, with Engrave-mode workaround named if collisions occurred"
    - "If any check failed: D-03 loop closed → fix committed to a Phase 2 module + regression test added; re-emitted cents.doricolib md5 recorded; failed check re-run and now passes (cosmetic-only UX-03 dense-passage failures may be documented as known limitations instead — D-03 carve-out)"
    - "Determinism contract preserved across any D-03 loop: PROJECT_NAMESPACE not rotated; key strings (sharp, flat, natural, <base><signed-cents>) not renamed"
    - "D-02: HALion only is the gate for Phase 3 sign-off. Dorico's stock playback engine is the sole engine under test. NotePerformer is not re-validated even if owned — Phase 4's README cites NotePerformer as separately-confirmed cent-accurate per published Scoring Notes commentary, without our re-test. No NotePerformer column, no NotePerformer setup, no NotePerformer fallback in this plan."
  artifacts:
    - path: ".planning/phases/03-dorico-import-playback-validation/03-VERIFICATION.md"
      provides: "Recorded results for PLAY-02, PLAY-03, UX-03 (extended by /gsd-verify-phase per D-04, building on Plan 03-01 results)"
  key_links:
    - from: "Accidentals panel entry (e.g., `Sharp +50`)"
      to: "selected notehead in flow"
      via: "click-to-apply from panel"
      pattern: "Write mode → select notehead → click accidental in panel"
    - from: "notehead with applied accidental"
      to: "tuner reading via HALion playback"
      via: "spacebar playback + tuner app capturing audio output"
      pattern: "Play → external tuner reads cents deviation from natural reference"
---

<objective>
Phase 3 manual-validation Plan 2 of 2. Confirm that every entry in the PLAY-03 named matrix plays back at its expected pitch within ±1¢ tolerance against HALion (Dorico's stock playback engine, per CONTEXT.md D-02), and document sparse-passage layout cleanliness plus dense-passage collision behavior for the named `Sharp -50` + `Natural +50` + `Flat +50` chord stack.

Purpose: Close PLAY-02 (panel ↔ playback wiring), PLAY-03 (named tuner matrix — the off-by-100 trap diagnostic surface), and UX-03 (sparse no-collision + dense documented). This is the time-intensive part of Phase 3; Plan 03-01 (panel-population + panel-search) ran first so cheap checks are out of the way.

Output: Tickable per-row tuner readings + sparse/dense observations recorded in 03-VERIFICATION.md (created/extended by /gsd-verify-phase). No code changes unless the D-03 pause-and-fix loop triggers — which, for non-cosmetic failures, includes adding a regression test BEFORE the fix lands.

Per CONTEXT.md D-01 and D-05, the plan IS the protocol — no separate walkthrough document. Plan 03-01 must be complete before this plan begins (Library Manager import + open key signature + `cents` tonality system already active).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md

This plan is `autonomous: false`. Every task is a manual user action against the user's installed Dorico Pro 6.x on macOS, with HALion as the playback engine and an external tuner (any reliable tuner — desktop app like TonalEnergy, AudioTuner, or even a hardware tuner that can read the audio output) as the measurement device. The executor presents tasks as a checklist; the user runs each step in Dorico, plays, reads the tuner, and reports the observed cent deviation.
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/03-dorico-import-playback-validation/03-CONTEXT.md
@.planning/research/PITFALLS.md

<interfaces>
<!-- Anchors under test (no code interfaces — manual validation phase). -->
- Production artifact: `cents.doricolib` at repo root (md5 `4cd707d2f4b10154a528b95e2ff5db9f` unless Plan 03-01's D-03 loop already updated it).
- Playback engine: HALion (Dorico stock, no setup required per CONTEXT.md D-02 + Pitfall 12).
- Reference pitch convention for this plan: use **C5** (middle-C-octave-up, MIDI 72, ~523.25 Hz nominal) as the anchor notehead unless a case calls for a different pitch. Place all spot-check notes on C5 of a piano staff or another sustained-tone instrument so the tuner has a clean steady-state to lock onto.
- Tuner tolerance: ±1¢ per PROJECT.md precision constraint.
- Re-emission CLI (only used if D-03 loop triggers): `python build.py --out cents.doricolib`.
- Test net (D-03 regression floor): 133 pytest tests in `tests/test_*.py`.
- Locked determinism contract (do NOT violate in any D-03 fix): PROJECT_NAMESPACE in `src/cents_generator/uuids.py`; key strings `sharp` / `flat` / `natural` / `<base><signed-cents>`.

PLAY-03 expected math (reproduced verbatim from CONTEXT.md so the user can verify each row without recomputing):
- `Sharp 0` (zero-dev) → +100¢ above natural reference.
- `Flat 0` (zero-dev) → -100¢ below natural reference.
- `Natural 0` (zero-dev) → 0¢ (the literal natural note).
- `Sharp +50` → +150¢ above natural (off-by-100 trap diagnostic).
- `Flat -7` → -107¢ below natural (off-by-100 trap diagnostic).
- `Sharp +99` → +199¢ (boundary).
- `Sharp -99` → +1¢ above natural (= 100 - 99) (boundary).
- `Flat +99` → -1¢ below natural (= -100 + 99) (boundary).
- `Flat -99` → -199¢ (boundary).
- `Natural +99` → +99¢ (boundary).
- `Natural -99` → -99¢ (boundary).
- `Sharp -50` ≡ `Natural +50` → both +50¢ above natural (enharmonic equivalent).
</interfaces>
</context>

<tasks>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 0: Stage tuner + reference natural for PLAY-03 matrix</name>
  <files></files>
  <read_first></read_first>
  <what-built>Plan 03-01 left Dorico in: open key signature, `cents` tonality system active, accidentals panel populated with 597 entries. This task stages the playback environment so every PLAY-03 row that follows can be measured the same way: same anchor pitch (C5), same instrument, same tuner.</what-built>
  <how-to-verify>
    1. In Write mode, select a sustained-tone-friendly instrument staff (piano or strings is fine). Pick a measure with empty space; insert a whole note on C5 (octave above middle C). This is the "reference natural" — every PLAY-03 row's expected cents value is measured RELATIVE to the tuner reading on this plain C5.
    2. Open a tuner app and route Dorico's audio to it. On macOS the simplest path: launch any tuner desktop app (TonalEnergy, AudioTuner, or browser-based Soundcorset etc.); use a virtual audio routing tool (BlackHole, Loopback) OR simply hold a microphone in front of the speakers. A built-in Mac mic next to the speakers reads cents accurately enough at ±1¢ for sustained tones.
    3. Press Play (Spacebar) on the C5 whole note. Tuner reads C5 with whatever default deviation (it should be very close to 0¢ with a brief attack-time wobble; let the steady-state stabilize). Record this reading as the "natural reference" baseline. Subsequent rows' expected deltas are measured FROM this baseline.
    4. Stop playback.
    5. (Optional but recommended) For each subsequent row, return to this baseline C5 if the tuner drifts; or apply each accidental to a fresh duplicate of the C5 whole note so you can compare side-by-side.
  </how-to-verify>
  <acceptance_criteria>Tuner is reading Dorico's audio output reliably; baseline C5 plays at ~0¢ deviation from concert C (steady-state, post-attack); subsequent accidental applications will be measurable against this baseline at ±1¢ resolution.</acceptance_criteria>
  <resume-signal>Reply "tuner staged, baseline C5 reads ___¢" with the observed steady-state baseline (typically near 0¢).</resume-signal>
  <done>PLAY-03 matrix tuner setup complete; ready to walk the named cases.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 1: PLAY-03 zero-dev — `Sharp` (zero-dev) plays +100¢ above natural</name>
  <files></files>
  <read_first></read_first>
  <what-built>PLAY-03 zero-deviation row 1 of 3. The plain `Sharp` (no signed-cents suffix; Class A glyph-only at SMuFL `0xE262`) is the standard sharp accidental, which raises pitch by 100¢ (one semitone). Verifying this confirms baseline panel ↔ playback wiring (PLAY-02) for the sharp base.</what-built>
  <how-to-verify>
    1. Select the C5 whole note (or a fresh duplicate).
    2. In the Accidentals panel, search/scroll to `Sharp` (the zero-dev one — no `+N` or `-N` suffix). Click to apply. The notehead now shows the standard sharp glyph, no cent label.
    3. Press Play. Read tuner steady-state.
  </how-to-verify>
  <acceptance_criteria>Tuner reads +100¢ ± 1¢ above the natural C5 baseline (i.e., a C♯5 / D♭5 — but specifically the played pitch is 100¢ above plain C5).</acceptance_criteria>
  <resume-signal>Reply "Sharp: tuner reads ___¢" with the observed value.</resume-signal>
  <done>PLAY-03 zero-dev Sharp row recorded.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: PLAY-03 zero-dev — `Flat` (zero-dev) plays -100¢ below natural</name>
  <files></files>
  <read_first></read_first>
  <what-built>PLAY-03 zero-deviation row 2 of 3. Plain `Flat` (Class A glyph-only at SMuFL `0xE260`) lowers pitch by 100¢.</what-built>
  <how-to-verify>
    1. Select the C5 whole note (or a fresh duplicate; remove any prior accidental).
    2. In the Accidentals panel, apply zero-dev `Flat`. Notehead shows the standard flat glyph, no cent label.
    3. Press Play. Read tuner steady-state.
  </how-to-verify>
  <acceptance_criteria>Tuner reads -100¢ ± 1¢ below the natural C5 baseline.</acceptance_criteria>
  <resume-signal>Reply "Flat: tuner reads ___¢" with the observed value.</resume-signal>
  <done>PLAY-03 zero-dev Flat row recorded.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: PLAY-03 zero-dev — `Natural` (zero-dev) plays 0¢ (literal note)</name>
  <files></files>
  <read_first></read_first>
  <what-built>PLAY-03 zero-deviation row 3 of 3. Plain `Natural` (Class A glyph-only at SMuFL `0xE261`) is the literal natural note — it MUST play at the same pitch as a notehead with no accidental at all (i.e., 0¢ from baseline).</what-built>
  <how-to-verify>
    1. Select the C5 whole note (or a fresh duplicate; remove any prior accidental).
    2. In the Accidentals panel, apply zero-dev `Natural`. Notehead shows the standard natural glyph, no cent label.
    3. Press Play. Read tuner steady-state.
  </how-to-verify>
  <acceptance_criteria>Tuner reads 0¢ ± 1¢ — the same as the unmarked C5 baseline from Task 0.</acceptance_criteria>
  <resume-signal>Reply "Natural: tuner reads ___¢" with the observed value.</resume-signal>
  <done>PLAY-03 zero-dev Natural row recorded; all three zero-deviation bases validated.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 4: PLAY-03 off-by-100 trap — `Sharp +50` plays +150¢ above natural</name>
  <files></files>
  <read_first></read_first>
  <what-built>PLAY-03 off-by-100 trap row 1 of 2. This is THE diagnostic case for Pitfall 1 (the off-by-100 trap that Phase 2's `pitch_delta_numerator()` defends against). If the central pitch helper had a bug treating `Sharp +50` as 50¢ instead of 150¢, this row catches it. Class B composite (sharp glyph + `+50` text label).</what-built>
  <how-to-verify>
    1. Select the C5 whole note (or a fresh duplicate; remove any prior accidental).
    2. In the panel, search `Sharp +50` (or scroll to it). Apply. Notehead shows sharp glyph + `+50` label above.
    3. Press Play. Read tuner steady-state.
  </how-to-verify>
  <acceptance_criteria>Tuner reads +150¢ ± 1¢ above the natural C5 baseline. NOT +50¢ (that would be the off-by-100 bug — fail and trigger D-03).</acceptance_criteria>
  <resume-signal>Reply "Sharp +50: tuner reads ___¢" with the observed value.</resume-signal>
  <done>PLAY-03 Sharp +50 row recorded; Pitfall 1 sharp-side defense confirmed.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 5: PLAY-03 off-by-100 trap — `Flat -7` plays -107¢ below natural</name>
  <files></files>
  <read_first></read_first>
  <what-built>PLAY-03 off-by-100 trap row 2 of 2. The flat-side mirror of the off-by-100 trap. Class B composite (flat glyph + `-7` text label). If `pitch_delta_numerator(flat, -7)` returned -7 instead of -107, this row catches it.</what-built>
  <how-to-verify>
    1. Select the C5 whole note (or a fresh duplicate; remove any prior accidental).
    2. Apply `Flat -7` from the panel.
    3. Press Play. Read tuner steady-state.
  </how-to-verify>
  <acceptance_criteria>Tuner reads -107¢ ± 1¢ below the natural C5 baseline. NOT -7¢ (that would be the off-by-100 bug — fail and trigger D-03).</acceptance_criteria>
  <resume-signal>Reply "Flat -7: tuner reads ___¢" with the observed value.</resume-signal>
  <done>PLAY-03 Flat -7 row recorded; Pitfall 1 flat-side defense confirmed.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 6: PLAY-03 boundary — `Sharp +99` plays +199¢ above natural</name>
  <files></files>
  <read_first></read_first>
  <what-built>PLAY-03 boundary row 1 of 6. Tests the upper edge of the sharp-side range. Class B composite (sharp glyph + `+99` text label). Combined with subsequent boundary rows this confirms the full -199¢..+199¢ range claimed by ROADMAP success criterion 1.</what-built>
  <how-to-verify>
    1. Select the C5 whole note (or a fresh duplicate; remove any prior accidental).
    2. Apply `Sharp +99` from the panel.
    3. Press Play. Read tuner steady-state.
  </how-to-verify>
  <acceptance_criteria>Tuner reads +199¢ ± 1¢ above natural C5 baseline.</acceptance_criteria>
  <resume-signal>Reply "Sharp +99: tuner reads ___¢" with the observed value.</resume-signal>
  <done>PLAY-03 Sharp +99 row recorded.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 7: PLAY-03 boundary — `Sharp -99` plays +1¢ above natural</name>
  <files></files>
  <read_first></read_first>
  <what-built>PLAY-03 boundary row 2 of 6. The lower edge of the sharp-side range — and a non-obvious math case (100 + (-99) = +1¢). If a user mis-derives the expected value, the off-by-100 trap can hide here. Class B composite.</what-built>
  <how-to-verify>
    1. Select the C5 whole note (or a fresh duplicate; remove any prior accidental).
    2. Apply `Sharp -99` from the panel.
    3. Press Play. Read tuner steady-state.
  </how-to-verify>
  <acceptance_criteria>Tuner reads +1¢ ± 1¢ above natural C5 baseline (i.e., very close to natural but barely sharp). Critically NOT -99¢ (that would be the off-by-100 bug — sharp-side negative cents collapsed to flat-side).</acceptance_criteria>
  <resume-signal>Reply "Sharp -99: tuner reads ___¢" with the observed value.</resume-signal>
  <done>PLAY-03 Sharp -99 row recorded; sharp-side overlap with natural confirmed.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 8: PLAY-03 boundary — `Flat +99` plays -1¢ below natural</name>
  <files></files>
  <read_first></read_first>
  <what-built>PLAY-03 boundary row 3 of 6. The upper edge of the flat-side range — mirror of Task 7 (-100 + 99 = -1¢). Class B composite (flat glyph + `+99` text label).</what-built>
  <how-to-verify>
    1. Select the C5 whole note (or a fresh duplicate; remove any prior accidental).
    2. Apply `Flat +99` from the panel.
    3. Press Play. Read tuner steady-state.
  </how-to-verify>
  <acceptance_criteria>Tuner reads -1¢ ± 1¢ below natural C5 baseline. NOT +99¢ (off-by-100 bug — flat-side positive cents collapsed to sharp-side).</acceptance_criteria>
  <resume-signal>Reply "Flat +99: tuner reads ___¢" with the observed value.</resume-signal>
  <done>PLAY-03 Flat +99 row recorded; flat-side overlap with natural confirmed.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 9: PLAY-03 boundary — `Flat -99` plays -199¢ below natural</name>
  <files></files>
  <read_first></read_first>
  <what-built>PLAY-03 boundary row 4 of 6. The lower edge of the flat-side range — full reach of the library's negative span (-100 + (-99) = -199¢). Class B composite.</what-built>
  <how-to-verify>
    1. Select the C5 whole note (or a fresh duplicate; remove any prior accidental).
    2. Apply `Flat -99` from the panel.
    3. Press Play. Read tuner steady-state.
  </how-to-verify>
  <acceptance_criteria>Tuner reads -199¢ ± 1¢ below natural C5 baseline.</acceptance_criteria>
  <resume-signal>Reply "Flat -99: tuner reads ___¢" with the observed value.</resume-signal>
  <done>PLAY-03 Flat -99 row recorded.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 10: PLAY-03 boundary — `Natural +99` plays +99¢ above natural</name>
  <files></files>
  <read_first></read_first>
  <what-built>PLAY-03 boundary row 5 of 6. The upper edge of the natural-base non-zero range (Class C — text label only, no base glyph). Cleanly tests `pitch_delta_numerator(natural, +99) = 99/1200`.</what-built>
  <how-to-verify>
    1. Select the C5 whole note (or a fresh duplicate; remove any prior accidental).
    2. Apply `Natural +99` from the panel. Notehead shows ONLY the `+99` text label (no base glyph — Class C).
    3. Press Play. Read tuner steady-state.
  </how-to-verify>
  <acceptance_criteria>Tuner reads +99¢ ± 1¢ above natural C5 baseline.</acceptance_criteria>
  <resume-signal>Reply "Natural +99: tuner reads ___¢" with the observed value.</resume-signal>
  <done>PLAY-03 Natural +99 row recorded; Class C upper boundary confirmed.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 11: PLAY-03 boundary — `Natural -99` plays -99¢ below natural</name>
  <files></files>
  <read_first></read_first>
  <what-built>PLAY-03 boundary row 6 of 6. The lower edge of the natural-base non-zero range (Class C). Mirror of Task 10.</what-built>
  <how-to-verify>
    1. Select the C5 whole note (or a fresh duplicate; remove any prior accidental).
    2. Apply `Natural -99` from the panel. Notehead shows ONLY the `-99` text label.
    3. Press Play. Read tuner steady-state.
  </how-to-verify>
  <acceptance_criteria>Tuner reads -99¢ ± 1¢ below natural C5 baseline.</acceptance_criteria>
  <resume-signal>Reply "Natural -99: tuner reads ___¢" with the observed value.</resume-signal>
  <done>PLAY-03 Natural -99 row recorded; Class C lower boundary confirmed; all six boundary cases done.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 12: PLAY-03 enharmonic — `Sharp -50` and `Natural +50` produce the same audible pitch</name>
  <files></files>
  <read_first></read_first>
  <what-built>PLAY-03 enharmonic-equivalent pair (Pitfall 10 confirmation). `Sharp -50` math: 100 + (-50) = +50¢. `Natural +50` math: 0 + 50 = +50¢. Both must produce IDENTICAL sounding pitches in HALion. Visually they remain distinct entries (a sharp glyph with `-50` label vs. a `+50` text-only label). This is the spelling-flexibility guarantee: composers can choose whichever notational variant fits the harmonic context.</what-built>
  <how-to-verify>
    1. Place TWO C5 whole notes side-by-side (or two voices on the same beat — separate noteheads anyway).
    2. Apply `Sharp -50` to the first note. Apply `Natural +50` to the second note.
    3. Press Play. Listen — they should sound identical.
    4. Confirm via tuner: each note plays at +50¢ ± 1¢ above natural C5 baseline. Specifically, Sharp -50 reads ~+50¢ AND Natural +50 reads ~+50¢ (within ±1¢ of each other).
  </how-to-verify>
  <acceptance_criteria>Both `Sharp -50` and `Natural +50` produce a tuner reading of +50¢ ± 1¢ above natural C5 baseline. Audibly indistinguishable in HALion. Visually distinct in the score (different accidental glyphs/labels).</acceptance_criteria>
  <resume-signal>Reply "enharmonic confirmed: Sharp -50 reads ___¢, Natural +50 reads ___¢" with both observed values.</resume-signal>
  <done>PLAY-03 enharmonic-equivalent pair validated; PLAY-03 named matrix complete; PLAY-02 panel ↔ playback wiring closed implicitly across all 12 tasks above.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 13: UX-03 sparse-passage no-collision check</name>
  <files></files>
  <read_first></read_first>
  <what-built>UX-03 sparse-passage requirement: in normal note spacing, cent labels must NOT collide with note heads or ledger lines. The `(-8, -12)` Class B offset and `(18, -12)` Class C offset (locked by Phase 2 VIS-02 / VIS-03) are designed for this case.</what-built>
  <how-to-verify>
    1. In Write mode, on a fresh measure with normal note spacing (default Dorico spacing — no manual adjustment), insert 1–2 noteheads with cent-labeled accidentals. Pick visually informative cases:
       - One Class B (e.g., `Sharp +14` on C5 — sharp glyph + small `+14` label).
       - One Class C (e.g., `Natural -50` on a note that sits on or near a ledger line — e.g., A5 above the treble staff to put a ledger line in play).
    2. Inspect visually:
       - Does the cent label collide with the notehead it's attached to?
       - Does the cent label collide with a neighboring notehead or stem?
       - Does the cent label collide with a ledger line?
       - Is the cent label still legible (not clipped, not overlapping unrelated glyphs)?
  </how-to-verify>
  <acceptance_criteria>NO collisions observed: cent labels sit cleanly above/beside their attached glyphs without overlapping note heads, stems, ledger lines, or neighboring elements. Labels are legible.</acceptance_criteria>
  <resume-signal>Reply "sparse: clean" or describe any collision observed (which case, which element collided).</resume-signal>
  <done>UX-03 sparse-passage cleanliness recorded.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 14: UX-03 dense-passage `Sharp -50` + `Natural +50` + `Flat +50` chord stack documentation</name>
  <files></files>
  <read_first></read_first>
  <what-built>UX-03 dense-passage requirement (the named hard case from ROADMAP success criterion 3). Three notes on a single beat producing two distinct sounding pitches: `Sharp -50` and `Natural +50` are enharmonic at +50¢; `Flat +50` is at -50¢. Three cent labels stack near each other on a single beat. Per CONTEXT.md D-03 cosmetic-only carve-out, if collisions occur this is documented (with Engrave-mode workaround named) rather than triggering the D-03 fix loop.</what-built>
  <how-to-verify>
    1. In Write mode, on a fresh beat in the same flow, insert a chord with three noteheads at three nearby pitches (e.g., C5, D5, E5 — close enough that their cent labels will be near each other). Use Voice 1 + Voice 2 + Voice 3 if needed to stack them on the same beat without notehead conflict, OR place them as a single chord on adjacent staff degrees.
    2. Apply `Sharp -50` to the first notehead, `Natural +50` to the second, `Flat +50` to the third.
    3. Inspect visually:
       - Do any cent labels (`-50`, `+50`, `+50`) collide with each other?
       - Do any cent labels collide with note heads or stems?
       - Do any cent labels collide with the base sharp/flat glyphs of neighboring notes?
       - Is each cent label still legible / attributable to its parent notehead?
    4. Record the observation. Two outcomes:
       - **Clean (no collisions):** Mark UX-03 dense-passage as PASS and proceed.
       - **Collisions occurred:** Document the specific collisions in 03-VERIFICATION.md AND name the Engrave-mode workaround: in Engrave mode, individual accidentals can be repositioned per-note via the Properties panel's accidental X/Y offset controls (Pitfall 9 — accidentals can't move vertically library-wide, but per-note Engrave-mode adjust is the documented escape hatch). Per CONTEXT.md D-03, this cosmetic-only failure is documented as a known limitation, NOT looped back into the D-03 fix path. Phase 4's README will carry the workaround prose.
  </how-to-verify>
  <acceptance_criteria>One of: (a) no collisions → mark PASS in 03-VERIFICATION.md; OR (b) collisions occurred → 03-VERIFICATION.md records the specific collisions observed AND names the Engrave-mode per-note adjust as the workaround (Properties panel → accidental offset). Either outcome closes UX-03 dense-passage per ROADMAP success criterion 3.</acceptance_criteria>
  <resume-signal>Reply "dense: clean" OR "dense: collisions observed — [brief description]; Engrave-mode workaround documented" with the observation.</resume-signal>
  <done>UX-03 dense-passage behavior documented; full UX-03 closes.</done>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 15: If any check above failed: D-03 pause-and-fix loop</name>
  <files></files>
  <read_first>
@.planning/phases/03-dorico-import-playback-validation/03-CONTEXT.md (re-read §decisions D-03 and Determinism contract; note the cosmetic-only carve-out applies ONLY to UX-03 dense-passage layout)
@.planning/research/PITFALLS.md (Pitfall 1 + Recovery Strategies section)
  </read_first>
  <what-built>This is a CONDITIONAL task. If every Task 1..14 acceptance criterion passed, this task is N/A. If any tuner check failed (PLAY-03 row outside ±1¢), or PLAY-02 panel ↔ playback wiring is broken, or UX-03 sparse-passage shows unexpected collisions, execute the D-03 pause-and-fix loop here. Cosmetic-only UX-03 dense-passage failures are EXEMPT (documented as known limitations per Task 14, not looped).</what-built>
  <how-to-verify>
    Diagnose the root cause. Map failure → responsible Phase 2 module:
    - Tuner reads off-by-100 anywhere (e.g., `Sharp +50` plays +50¢ instead of +150¢) → bug in `src/cents_generator/pitch.py` `pitch_delta_numerator()`. Add a parametrized test in `tests/test_pitch.py` covering the failing input, then fix.
    - Tuner reads correct delta but accidental rendered on wrong class (e.g., `Natural +50` shows a base glyph it shouldn't) → bug in `src/cents_generator/compose.py` class dispatch. Add a snapshot/structural test in `tests/test_cents_snapshot.py` or `tests/test_cents_structural.py`, then fix.
    - Accidental imports fine but plays at standard pitch (Pitfall 3 silent text-component drop) → suspect `src/cents_generator/emit.py` formatting drift. Add an emit-layer test, then fix.
    - Panel surfaces an entry whose name doesn't match its `pitchDeltaFromNatural` → orchestrator dedup/composition bug in `src/cents_generator/main.py`. Add a structural test, then fix.
    - UX-03 SPARSE collision (unexpected; Phase 2 VIS-02/VIS-03 offsets are designed for sparse cleanliness) → bug in `src/cents_generator/compose.py` offsets `(-8, -12)` / `(18, -12)`. This is rare but if it surfaces, fix here. Add a snapshot test asserting the literal offsets.

    Mandatory fix protocol (same as Plan 03-01 Task 9):
    1. Add a regression test to the appropriate `tests/test_*.py` file BEFORE the fix lands. Test must FAIL on current code and PASS on fix.
    2. Implement the fix in the responsible module.
    3. Run full test suite: `pytest` — MUST pass at least 134/134 (133 existing + at least 1 new regression).
    4. Re-emit: `python build.py --out cents.doricolib`. Record new md5 in 03-VERIFICATION.md.
    5. Re-import via Library Manager (Plan 03-01 Task 2 procedure). Update-in-place semantics rely on stable entityIDs.
    6. Re-run ONLY the failed checks from Tasks 1..14.

    HARD CONSTRAINTS (violations require user escalation):
    - MUST NOT rotate `PROJECT_NAMESPACE` in `src/cents_generator/uuids.py`. Locked forever.
    - MUST NOT rename key strings (`sharp`, `flat`, `natural`, `<base><signed-cents>`).
    - MUST NOT modify `src/cents_generator/uuids.py`, `src/cents_generator/constants.py`, or `src/cents_generator/entities.py` absent a confirmed schema-level bug (out of D-03 scope).

    COSMETIC CARVE-OUT: UX-03 dense-passage layout failures (collisions in the `Sharp -50` + `Natural +50` + `Flat +50` chord stack ONLY) are DOCUMENTED per Task 14, not looped. The Engrave-mode per-note adjust is the named workaround (Pitfall 9). This carve-out does NOT extend to:
    - UX-03 sparse-passage collisions (failure → loop)
    - Any PLAY-02 / PLAY-03 tuner failure (failure → loop)
    - Any Plan 03-01 panel / search failure (failure → loop, retroactively if it surfaces here)
  </how-to-verify>
  <acceptance_criteria>EITHER (a) no checks failed → task is N/A, mark "skipped — all checks passed" in 03-VERIFICATION.md; OR (b) failures were UX-03 dense-passage cosmetic-only → documented as known limitation per Task 14, no code change, mark D-03 loop "not triggered (cosmetic carve-out)"; OR (c) every previously-failed non-cosmetic check now passes after fix → 134+/134+ pytest tests pass; new cents.doricolib md5 recorded; PROJECT_NAMESPACE unchanged (verified by `grep -n PROJECT_NAMESPACE src/cents_generator/uuids.py` matching pre-loop value); fix and regression test committed together.</acceptance_criteria>
  <resume-signal>Reply "skipped" if no failures, "cosmetic-only — documented" if only UX-03 dense, or describe failure + fix + new md5 + retest result.</resume-signal>
  <done>Either no D-03 loop needed, or the loop closed cleanly with regression coverage and preserved determinism contract, or cosmetic-only failures documented per the carve-out.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Dorico HALion → tuner audio | Pitch correctness flows from `pitchDeltaFromNatural` (XML) through HALion's microtuning engine to the audio output the tuner reads. A bug at any layer (XML rational drift, HALion sample-mapping interpolation error) shows up here. |
| Phase 2 generator → Phase 3 artifact | Same as Plan 03-01: `cents.doricolib` is the gold-standard input; integrity asserted by md5. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-3-06 | Tampering | pitch_delta_numerator() math | mitigate | Tasks 4 + 5 (off-by-100 trap diagnostic) and Tasks 7 + 8 (boundary edge math) are explicit detection probes. Failure triggers D-03 with regression-test-first policy. |
| T-3-07 | Tampering | Phase 2 modules during D-03 loop | mitigate | Task 15 mandates regression-test-first; locked PROJECT_NAMESPACE and key strings prevent silent identity rotation; 133-test pytest suite is the regression floor. |
| T-3-08 | Spoofing | enharmonic spelling integrity | mitigate | Task 12 explicitly verifies Sharp -50 ≡ Natural +50 audibly identical (Pitfall 10). Two distinct entityIDs producing the same sounding pitch is a feature, not a bug — but if they disagree, that's a math bug caught here. |
| T-3-09 | Denial of Service | dense-passage layout | accept | Per CONTEXT.md D-03 cosmetic carve-out + ROADMAP success criterion 3, dense-passage collisions are an accepted limitation with named workaround (Engrave-mode per-note adjust, Pitfall 9). Phase 4 README documents. |
| T-3-10 | Repudiation | Validation results | mitigate | All 12 PLAY-03 rows + UX-03 sparse + UX-03 dense recorded in 03-VERIFICATION.md (D-04) with Dorico build version (Plan 03-01 Task 1) and re-emitted md5 if D-03 loop fired. |

ASVS L1 — applicable controls: integrity verification (md5), input validation (xmllint pre-flight in Plan 03-01), regression-test-first policy in D-03 loop. No auth, no network, no PII handling.
</threat_model>

<verification>
Plan 03-02 closes when every Task 0..14 acceptance criterion is satisfied (and Task 15 is skipped, cosmetic-only documented, or has closed cleanly with re-emitted artifact + new test). The user records each result in 03-VERIFICATION.md (extending Plan 03-01's results). Plan 03-02 results: PLAY-03 12-row matrix tuner readings (3 zero-dev + 2 off-by-100 + 6 boundary + 1 enharmonic), UX-03 sparse outcome, UX-03 dense outcome (with workaround named if collisions occurred), D-03 loop disposition.
</verification>

<success_criteria>
1. All 12 PLAY-03 named rows pass tuner spot-check at ±1¢ tolerance against HALion in Dorico Pro 6.x.
2. PLAY-02 (panel ↔ playback wiring) closes implicitly: every PLAY-03 row exercised click-to-apply from panel → played pitch reflects the cent delta.
3. UX-03 sparse-passage: no collisions between cent labels and note heads / ledger lines / neighbors.
4. UX-03 dense-passage: behavior documented in 03-VERIFICATION.md (clean OR with Engrave-mode workaround named if collisions occurred).
5. If any non-cosmetic check failed: D-03 loop closed with regression test + preserved determinism contract.
</success_criteria>

<output>
After completion, the user (via /gsd-verify-phase) records Plan 03-02 results in `.planning/phases/03-dorico-import-playback-validation/03-VERIFICATION.md` (extending Plan 03-01 results into a single file per CONTEXT.md D-04). No SUMMARY needed for a manual-validation plan unless the D-03 loop fired (in which case create `.planning/phases/03-dorico-import-playback-validation/03-02-SUMMARY.md` documenting the failure → fix → re-emit cycle).
</output>
