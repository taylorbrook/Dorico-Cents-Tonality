---
phase: 3
plan: 01
title: Library Manager import + panel population + panel-search ergonomics
type: execute
wave: 1
depends_on: []
files_modified: []
autonomous: false
requirements: [UX-01, UX-02]
must_haves:
  truths:
    - "xmllint --noout cents.doricolib exits 0 (well-formedness pre-flight, Pitfall 4 defense)"
    - "Library Manager import of cents.doricolib on Dorico Pro 6.x macOS completes without error"
    - "After Shift+K → `open` and switching the flow's tonality system to `cents`, the Key Signatures, Tonality Systems, and Accidentals panel populates with exactly 597 accidentals"
    - "Panel-search query `+14` returns exactly 3 matches: `Natural +14`, `Sharp +14`, `Flat +14`"
    - "Panel-search query `Sharp -` returns exactly 99 matches (Sharp -1 through Sharp -99)"
    - "Panel-search query `Flat +50` returns exactly 1 match: `Flat +50`"
    - "Panel-search query `Natural` returns 199 matches (Natural 0 plus Natural +1..+99 and Natural -1..-99)"
    - "All four UX-02 queries return their expected match counts in usable interactive time (no perceptible lag, no Dorico beachball)"
    - "Dorico Pro 6.x build version recorded in 03-VERIFICATION.md"
    - "If any check above failed: D-03 loop closed → fix committed to a Phase 2 module + regression test added; re-emitted cents.doricolib md5 recorded; failed check re-run and now passes"
    - "Determinism contract preserved across any D-03 loop: PROJECT_NAMESPACE not rotated; key strings (sharp, flat, natural, <base><signed-cents>) not renamed"
    - "D-01: No test scaffold artifact is created. The plan IS the protocol — the user opens an existing Dorico project, imports cents.doricolib via Library Manager, and walks the enumerated checks below top-to-bottom. No tools/build_test_score.py, no markdown walkthrough, no validation-scratch.dorico template."
    - "D-05: Two-plan split (03-01 panel checks + 03-02 playback matrix), wave 1 → wave 2 sequential. Not three-wave fan-out. Plan 03-01 = cheap-fast UX-01 + UX-02 panel-population and panel-search; Plan 03-02 = time-intensive PLAY-02 + PLAY-03 + UX-03 tuner matrix and layout."
  artifacts:
    - path: ".planning/phases/03-dorico-import-playback-validation/03-VERIFICATION.md"
      provides: "Recorded results for UX-01 + UX-02 with Dorico build version (created/extended by /gsd-verify-phase per D-04)"
  key_links:
    - from: "cents.doricolib (repo root)"
      to: "Dorico Pro 6.x Library Manager"
      via: "manual Library Manager import"
      pattern: "Library → Library Manager → Import"
    - from: "open key signature in flow"
      to: "Key Signatures, Tonality Systems, and Accidentals panel"
      via: "tonality system switch to `cents`"
      pattern: "Setup mode → flow → tonality system: cents"
---

<objective>
Phase 3 manual-validation Plan 1 of 2. Confirm cents.doricolib imports cleanly into the user's installed Dorico Pro 6.x on macOS, that the accidentals panel populates with all 597 entries at the unprecedented 597-entry scale, and that panel-search ergonomics work for the four named UX-02 queries.

Purpose: Close UX-01 and UX-02 by hand against Dorico's UI. This plan covers the cheap, fast checks; Plan 03-02 carries the time-intensive tuner matrix and collision documentation. Per CONTEXT.md D-01 and D-05, the plan IS the protocol — no separate walkthrough document.

Output: Tickable checklist results captured in 03-VERIFICATION.md (created by /gsd-verify-phase after both plans complete). No code changes unless the D-03 pause-and-fix loop triggers.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md

This plan is `autonomous: false`. Every task is a manual user action against the user's installed Dorico Pro 6.x on macOS. The executor presents tasks as a checklist; the user runs each step in Dorico and reports observed values.
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/03-dorico-import-playback-validation/03-CONTEXT.md
@.planning/research/PITFALLS.md
@.planning/research/FEATURES.md

<interfaces>
<!-- Anchors under test (no code interfaces — manual validation phase). -->
- Production artifact: `cents.doricolib` at repo root (1,261,618 bytes, md5 `4cd707d2f4b10154a528b95e2ff5db9f`).
- Re-emission CLI (only used if D-03 loop triggers): `python build.py --out cents.doricolib`
- Pre-flight tool: `xmllint --noout cents.doricolib` (must exit 0).
- Test net (D-03 regression floor): 133 pytest tests in `tests/test_*.py`.
- Locked determinism contract (do NOT violate in any D-03 fix): PROJECT_NAMESPACE in `src/cents_generator/uuids.py`; key strings `sharp` / `flat` / `natural` / `<base><signed-cents>`.

Total entity count: 1411 = 1 Temperament + 1 AccidentalSystem + 1 TonalitySystem + 597 AccidentalDefinitions + 597 CompositeDefinitions + 3 Glyphs + 198 Texts. Panel surfaces the 597 accidentals.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 0: Pre-flight well-formedness check (Pitfall 4 defense, free)</name>
  <files></files>
  <read_first></read_first>
  <action>From the repo root, run `xmllint --noout cents.doricolib` in a terminal. This is a 1-second well-formedness sanity check before opening Dorico — catches any catastrophic XML corruption that would lead to a silent Library Manager rejection or, worse, a launch crash if the file ever lands in DefaultLibraryAdditions/. macOS ships xmllint by default, no install needed.</action>
  <verify>
    <automated>xmllint --noout cents.doricolib; echo "exit=$?"</automated>
  </verify>
  <acceptance_criteria>`xmllint --noout cents.doricolib` exits 0 (no output, no errors printed). If it prints any parser error, STOP — do not open Dorico — trigger the D-03 loop and fix the emit-layer bug before proceeding.</acceptance_criteria>
  <done>xmllint exits 0; cents.doricolib confirmed well-formed; safe to import.</done>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 1: Record Dorico Pro 6.x build version (D-04 citation source)</name>
  <files></files>
  <read_first></read_first>
  <what-built>cents.doricolib has been pre-flight-validated in Task 0. Now we capture the exact Dorico build the user is testing against, so 03-VERIFICATION.md can cite it (D-04) and Phase 4's README can name "validated against Dorico Pro 6.x.YY" precisely.</what-built>
  <how-to-verify>
    1. Open Dorico Pro 6.x on macOS.
    2. Menu bar: Dorico → About Dorico.
    3. Read the version string (e.g., `6.2.20`).
    4. Note it down for 03-VERIFICATION.md.
  </how-to-verify>
  <acceptance_criteria>Recorded Dorico build version: __________ (filled in by user, e.g., `6.2.20`).</acceptance_criteria>
  <resume-signal>Reply with the version string (e.g., "6.2.20") to continue.</resume-signal>
  <done>Dorico build version captured for inclusion in 03-VERIFICATION.md.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Library Manager import of cents.doricolib (UX-01 prerequisite)</name>
  <files></files>
  <read_first></read_first>
  <what-built>cents.doricolib (1,261,618 bytes, md5 `4cd707d2f4b10154a528b95e2ff5db9f`) is the Phase 2 production artifact at the repo root. This task imports it into Dorico via Library Manager — the safer of the two install paths (Pitfall 4: Library Manager fails gracefully; DefaultLibraryAdditions/ can fail fatally on launch).</what-built>
  <how-to-verify>
    1. In Dorico, open any existing project OR create a new empty project (any single instrument is fine; a piano or a sustained-tone instrument like Strings → Violin is convenient for the Plan 03-02 tuner work to follow, but any project works for this plan's panel checks).
    2. Menu bar: Library → Library Manager.
    3. In the Library Manager dialog, click `Import…` (or the equivalent button — the exact label varies slightly by point release).
    4. Navigate to the repo root and select `cents.doricolib`.
    5. Confirm the import. Library Manager should display the imported tonality system entry without error.
    6. Close Library Manager.
  </how-to-verify>
  <acceptance_criteria>Library Manager import completes without an error dialog. The imported library appears in the Library Manager list as a tonality-system bearing entry. NO Dorico crash, NO "failed to parse" error, NO silent skip.</acceptance_criteria>
  <resume-signal>Reply "imported" if Library Manager accepted the file, or describe the error message verbatim.</resume-signal>
  <done>cents.doricolib registered in the project's library; ready for tonality-system selection.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Apply open key signature + switch flow tonality system to `cents` (Pitfall 5 defense)</name>
  <files></files>
  <read_first></read_first>
  <what-built>The library is imported. Now we cross the #1 silent-failure gate for any Dorico custom tonality system: the active flow MUST be using an open or atonal key signature for custom-accidental input to be available. Per CONTEXT.md D-01, this is the FIRST action in Phase 3's manual flow.</what-built>
  <how-to-verify>
    1. Switch to Write mode if not already there (Cmd+2 on macOS).
    2. Select the first beat of the first instrument's first bar.
    3. Press `Shift+K` to open the key signatures popover.
    4. Type `open` and press Enter (or Return). The flow now uses an open/atonal key signature.
    5. Switch to Setup mode (Cmd+1).
    6. In the Players or Flows panel (depending on Dorico 6.x layout), find the tonality system selector for the current flow. (Typically: Setup → flow row → tonality system dropdown OR Library → Tonality Systems for the flow.)
    7. Select `cents` from the dropdown.
    8. Switch back to Write mode (Cmd+2).
  </how-to-verify>
  <acceptance_criteria>Open key signature applied (no key-signature glyph appears at staff start, or the flow indicates "open"); tonality system selector reads `cents`; no error dialogs; Dorico has not crashed.</acceptance_criteria>
  <resume-signal>Reply "open + cents applied" when both are in place, or describe what went wrong.</resume-signal>
  <done>Flow is in open key signature with the `cents` tonality system active — accidentals panel will now surface custom entries.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 4: Verify panel populates with all 597 accidentals (UX-01)</name>
  <files></files>
  <read_first></read_first>
  <what-built>With open key signature + `cents` tonality system active, the Key Signatures, Tonality Systems, and Accidentals panel must surface every one of the 597 accidentals (3 zero-deviation + 594 non-zero). This is UX-01.</what-built>
  <how-to-verify>
    1. Open the Key Signatures, Tonality Systems, and Accidentals panel (right side of Write mode; if hidden, View → Panels → … or use the right-edge panel toggle).
    2. Locate the Accidentals section / picker. The 597 entries should populate.
    3. Verify by spot-checks (do all three):
       - The three zero-deviation entries are present: `Sharp`, `Flat`, `Natural` (no signed-cents suffix on these).
       - The signed-cents boundary entries are present: `Sharp +99`, `Sharp -99`, `Flat +99`, `Flat -99`, `Natural +99`, `Natural -99`.
       - At least one mid-range entry per base: `Sharp +14`, `Flat -50`, `Natural -7`.
    4. If Dorico's panel exposes a count somewhere (header, status bar), record it. If not, the spot-checks above are the practical confirmation that the full sweep is present.
  </how-to-verify>
  <acceptance_criteria>Panel surfaces all 597 accidentals (verified by: (a) all three zero-dev entries `Sharp`, `Flat`, `Natural` visible; (b) all six boundary entries `Sharp ±99`, `Flat ±99`, `Natural ±99` visible; (c) all three mid-range probes `Sharp +14`, `Flat -50`, `Natural -7` visible). NO crash, NO "loading…" stuck state, NO truncation indicator.</acceptance_criteria>
  <resume-signal>Reply "597 confirmed" with results, or list which spot-checks failed.</resume-signal>
  <done>Panel population confirmed at 597-entry scale — UX-01 closes pending Plan 03-02 sign-off.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 5: Panel-search query `+14` returns 3 matches (UX-02 query 1 of 4)</name>
  <files></files>
  <read_first></read_first>
  <what-built>UX-02 query 1: `+14` is a substring search that should match every accidental whose name contains the literal `+14`. There are exactly 3 such accidentals — one per base.</what-built>
  <how-to-verify>
    1. In the Accidentals panel, click into the search/filter input.
    2. Type the literal string `+14` (plus sign, then `14`).
    3. Note the visible matching rows.
  </how-to-verify>
  <acceptance_criteria>Exactly 3 matches visible: `Natural +14`, `Sharp +14`, `Flat +14`. Search returns in usable interactive time (no perceptible lag, no Dorico beachball).</acceptance_criteria>
  <resume-signal>Reply "3 matches: Natural +14, Sharp +14, Flat +14" or describe what you observed.</resume-signal>
  <done>UX-02 query `+14` validated.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 6: Panel-search query `Sharp -` returns 99 matches (UX-02 query 2 of 4)</name>
  <files></files>
  <read_first></read_first>
  <what-built>UX-02 query 2: `Sharp -` (with the trailing space and hyphen literal) should match every Sharp-base accidental with a negative cents value: `Sharp -1` through `Sharp -99` = 99 entries. It should NOT match `Sharp` (zero-dev, no hyphen) or `Sharp +N`.</what-built>
  <how-to-verify>
    1. Clear the search input.
    2. Type the literal string `Sharp -` (capital S, lowercase `harp`, space, hyphen).
    3. Verify the visible matching rows. Spot-check that the first match is `Sharp -1` (or `Sharp -99` depending on sort order — both ends matter), and scrolling reveals the full 1..99 range.
  </how-to-verify>
  <acceptance_criteria>Exactly 99 matches visible (Sharp -1 through Sharp -99). NO `Sharp` (zero-dev) appears. NO `Sharp +N` appears. Search returns in usable interactive time.</acceptance_criteria>
  <resume-signal>Reply "99 matches: Sharp -1 through Sharp -99" or describe observed count + endpoints.</resume-signal>
  <done>UX-02 query `Sharp -` validated.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 7: Panel-search query `Flat +50` returns 1 match (UX-02 query 3 of 4)</name>
  <files></files>
  <read_first></read_first>
  <what-built>UX-02 query 3: `Flat +50` is the highest-specificity probe — exactly one accidental in the library matches it.</what-built>
  <how-to-verify>
    1. Clear the search input.
    2. Type the literal string `Flat +50`.
    3. Verify the result.
  </how-to-verify>
  <acceptance_criteria>Exactly 1 match visible: `Flat +50`. Search returns instantly.</acceptance_criteria>
  <resume-signal>Reply "1 match: Flat +50" or describe what you observed.</resume-signal>
  <done>UX-02 query `Flat +50` validated.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 8: Panel-search query `Natural` returns 199 matches (UX-02 query 4 of 4)</name>
  <files></files>
  <read_first></read_first>
  <what-built>UX-02 query 4: `Natural` is the broadest probe. It matches the zero-deviation `Natural` (1) plus every Natural-base non-zero entry: `Natural +1` through `Natural +99` (99) and `Natural -1` through `Natural -99` (99) = 199 total. This is the worst case for panel-search performance at the 597-entry scale.</what-built>
  <how-to-verify>
    1. Clear the search input.
    2. Type the literal string `Natural`.
    3. Confirm match count by:
       - Checking endpoints exist: `Natural` (zero-dev), `Natural +99`, `Natural -99` all visible (scroll if needed).
       - Confirming no `Sharp` or `Flat` rows leak into the result.
       - If Dorico shows a count, record it. If not, spot-check 5+ values across the range (`Natural -50`, `Natural -7`, `Natural`, `Natural +14`, `Natural +50`, `Natural +99`) to confirm the sweep is intact.
  </how-to-verify>
  <acceptance_criteria>199 matches visible (Natural zero-dev + Natural +1..+99 + Natural -1..-99). NO Sharp or Flat rows appear. Search returns in usable interactive time (no perceptible lag, no Dorico beachball — this is the panel-search ergonomics worst case).</acceptance_criteria>
  <resume-signal>Reply "199 matches confirmed (endpoints + spot-checks pass)" or describe observed behavior + any lag noticed.</resume-signal>
  <done>UX-02 query `Natural` validated; UX-02 closes pending Plan 03-02 sign-off.</done>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 9: If any check above failed: D-03 pause-and-fix loop</name>
  <files></files>
  <read_first>
@.planning/phases/03-dorico-import-playback-validation/03-CONTEXT.md (re-read §decisions D-03 and Determinism contract)
@.planning/research/PITFALLS.md (Pitfall-to-Phase Mapping + Recovery Strategies)
  </read_first>
  <what-built>This is a CONDITIONAL task. If every Task 0..8 check passed, this task is N/A — skip and proceed to Plan 03-02. If any check FAILED, execute the D-03 pause-and-fix loop here.</what-built>
  <how-to-verify>
    Diagnose the root cause of the failure. Map the failure to the responsible Phase 2 module:
    - Math errors (off-by-100 anywhere, wrong delta) → `src/cents_generator/pitch.py`
    - Class-dispatch / naming / offset errors (zero-dev rendering with a label, wrong panel name format, wrong x/yOffset) → `src/cents_generator/compose.py`
    - XML formatting drift / Dorico parse failure / silent text-component drop → `src/cents_generator/emit.py`
    - Panel showing wrong total / missing entries / dedup error → `src/cents_generator/main.py` (orchestrator)

    Mandatory fix protocol:
    1. Add a regression test to the appropriate `tests/test_*.py` file BEFORE the fix lands. The test must FAIL on the current code and PASS on the fix. Files: `tests/test_pitch.py`, `tests/test_cents_structural.py`, `tests/test_cents_snapshot.py`, `tests/test_determinism.py`.
    2. Implement the fix in the responsible module.
    3. Run the full test suite: `pytest` — it MUST pass 134/134 (133 existing + 1 new regression).
    4. Re-emit: `python build.py --out cents.doricolib`. Verify md5 changes (because content changed) but record the new md5 for 03-VERIFICATION.md citation.
    5. Re-import via Library Manager (Task 2 procedure). Library Manager update-in-place semantics rely on stable entityIDs (Pitfall 6 + determinism contract).
    6. Re-run ONLY the failed checks from Tasks 4..8.

    HARD CONSTRAINTS (violations require escalation, not silent commit):
    - MUST NOT rotate `PROJECT_NAMESPACE` in `src/cents_generator/uuids.py`. The constant is locked forever; rotating it breaks update-in-place for every existing user import (Pitfall 6).
    - MUST NOT rename key strings (`sharp`, `flat`, `natural`, `<base><signed-cents>`) in any file under `src/cents_generator/`. Renaming changes derived UUIDs, breaking re-import.
    - MUST NOT modify `src/cents_generator/uuids.py`, `src/cents_generator/constants.py`, or `src/cents_generator/entities.py` unless a deep schema/structural bug is confirmed (which is far beyond D-03's scope and requires user escalation).
    - Cosmetic-only carve-out (UX-03 dense-passage layout) does NOT apply to this plan — Plan 03-01 is panel-population + panel-search, both of which are correctness checks, not cosmetic.
  </how-to-verify>
  <acceptance_criteria>EITHER (a) no checks failed → task is N/A, mark "skipped — all checks passed" in 03-VERIFICATION.md; OR (b) every previously-failed check now passes after the fix → 134+/134+ pytest tests pass; new cents.doricolib md5 recorded; PROJECT_NAMESPACE unchanged (verified by `grep -n PROJECT_NAMESPACE src/cents_generator/uuids.py` matching pre-loop value); fix and regression test committed together.</acceptance_criteria>
  <resume-signal>Reply "skipped" if no failures, or describe the failure + fix + new md5 + retest result.</resume-signal>
  <done>Either no D-03 loop needed, or the loop closed cleanly with regression coverage and preserved determinism contract.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Repo → Dorico Library Manager | A `.doricolib` XML file crosses from the build workspace into Dorico's library subsystem; malformed XML can crash Dorico on launch if dropped in `DefaultLibraryAdditions/`, or silently fail to import via Library Manager. |
| Phase 2 generator → Phase 3 artifact | The build artifact is treated as the gold-standard input; integrity is asserted by md5 + xmllint pre-flight. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-3-01 | Tampering | cents.doricolib at repo root | mitigate | Task 0 runs `xmllint --noout cents.doricolib` (well-formedness pre-flight, Pitfall 4 defense). md5 (`4cd707d2f4b10154a528b95e2ff5db9f`) recorded in STATE.md and 03-VERIFICATION.md; any deviation is detected before import. |
| T-3-02 | Tampering | Phase 2 modules during D-03 loop | mitigate | Task 9 mandates regression-test-first policy; 133-test pytest suite is the regression floor; locked `PROJECT_NAMESPACE` and locked key strings prevent silent identity rotation. |
| T-3-03 | Denial of Service | Dorico Pro 6.x stability | accept | Library Manager (not `DefaultLibraryAdditions/`) is the chosen import path per CONTEXT.md (Pitfall 4); failure mode is graceful (error dialog), not launch crash. Risk is low. |
| T-3-04 | Information Disclosure | none | accept | No PII, no auth, no network — `.doricolib` is a single-user local XML file. ASVS L1 not applicable. |
| T-3-05 | Repudiation | Validation results | mitigate | All results recorded in 03-VERIFICATION.md (D-04) with Dorico build version (Task 1) and re-emitted md5 if D-03 loop fired (Task 9). Phase 4 README cites this artifact. |

ASVS L1 — applicable controls: integrity verification (md5), input validation (xmllint pre-flight). No auth, no network, no PII handling.
</threat_model>

<verification>
Plan 03-01 closes when every Task 0..8 acceptance criterion is satisfied (and Task 9 is either skipped or has closed cleanly with a re-emitted artifact and new test). The user records each result in 03-VERIFICATION.md (created by /gsd-verify-phase after Plan 03-02 also completes). Plan 03-01 results: pre-flight pass/fail, Dorico build version, import success, panel population (UX-01), four panel-search match counts (UX-02 1..4), D-03 loop disposition.
</verification>

<success_criteria>
1. `xmllint --noout cents.doricolib` exits 0.
2. Library Manager import completes without error on the user's installed Dorico Pro 6.x macOS.
3. After Shift+K → `open` and tonality system switch to `cents`, the Accidentals panel surfaces all 597 entries (verified by zero-dev triplet + ±99 boundary sextet + 3 mid-range probes).
4. Panel-search match counts: `+14` → 3, `Sharp -` → 99, `Flat +50` → 1, `Natural` → 199.
5. All searches return in usable interactive time (no beachball, no perceptible lag).
6. Dorico build version captured for 03-VERIFICATION.md.
7. If any check fails: D-03 loop closes with regression test + preserved determinism contract.
</success_criteria>

<output>
After completion, the user (via /gsd-verify-phase) records Plan 03-01 results in `.planning/phases/03-dorico-import-playback-validation/03-VERIFICATION.md`. No SUMMARY needed for a manual-validation plan unless the D-03 loop fired (in which case create `.planning/phases/03-dorico-import-playback-validation/03-01-SUMMARY.md` documenting the failure → fix → re-emit cycle).
</output>
