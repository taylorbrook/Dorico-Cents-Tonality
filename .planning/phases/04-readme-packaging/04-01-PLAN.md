---
phase: 04-readme-packaging
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - README.md
autonomous: true
requirements:
  - DIST-01
tags:
  - documentation
  - distribution
  - readme
must_haves:
  truths:
    - "A user reading the README knows Dorico Pro 6.0+ is required before any install steps"
    - "A user reading the README knows to use Library Manager as the primary install path"
    - "A user reading the README knows to remove the file from DefaultLibraryAdditions/ if Dorico fails to launch"
    - "A user reading the README sees Shift+K → 'open' as a step in the first-note walkthrough"
    - "A user reading the README can find naming-convention examples (Sharp +14, Flat -50, Natural -7)"
    - "A user with a silent failure can find the open-key-signature gotcha as the first troubleshooting item"
    - "A user using Kontakt/SWAM/Falcon knows microtonal playback may not work and HALion/NotePerformer is the validated path"
    - "A user changing fonts knows font.defaulttext can break cent labels"
    - "A user on Dorico 5/Elements/SE knows the file will not load"
    - "A user can find the LICENSE referenced in the README"
  artifacts:
    - path: "README.md"
      provides: "User-facing install + first-note + troubleshooting documentation"
      min_lines: 100
  key_links:
    - from: "README.md"
      to: "LICENSE"
      via: "Markdown link in License section"
      pattern: "LICENSE"
    - from: "README.md"
      to: "cents.doricolib"
      via: "Package contents reference"
      pattern: "cents\\.doricolib"
---

<objective>
Author the user-facing README.md at the repo root that ships alongside `cents.doricolib` and `LICENSE`. The README is the entire user experience surface for v1 — it must front-load the Dorico Pro 6.0+ requirement, present Library Manager as the primary install path, document the `DefaultLibraryAdditions/` power-user path with an explicit launch-crash recovery instruction, walk a first-time user through writing a cent-accurate note (including the Shift+K open key signature step that is the #1 silent failure), and provide troubleshooting that addresses the four critical pitfalls (open-key-sig gotcha, third-party VST limits, font-override caveat, version compatibility).

Purpose: Close DIST-01. A user receiving only `cents.doricolib`, README, and LICENSE must be able to install on Dorico Pro 6.x and write their first cent-accurate note without abandoning the library on a silent failure.

Output: `README.md` at the repo root with the ten sections enumerated in DIST-01, in order, containing every required literal string.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@./CLAUDE.md
@.planning/phases/03-dorico-import-playback-validation/03-01-SUMMARY.md
@.planning/phases/03-dorico-import-playback-validation/03-02-SUMMARY.md

<interfaces>
<!-- Authoritative content sources for this README. -->
<!-- All literal strings, paths, version numbers, and citations come from these files. -->
<!-- Do NOT invent install paths, version numbers, or playback claims. -->

From CLAUDE.md (this project's authoritative tech-stack research):
- macOS user library path: `~/Library/Application Support/Steinberg/Dorico 6/DefaultLibraryAdditions/`
- Windows user library path: `%APPDATA%\Steinberg\Dorico 6\DefaultLibraryAdditions\`
- Dorico target: Pro 6.0+
- Compatibility matrix:
  - Dorico Pro 6.0–6.2.x: YES (target)
  - Dorico Pro 5.x: NO
  - Dorico Pro 4.x and below: NO
  - Dorico Elements 6.x: PARTIAL (library import may succeed, tonality-system editing requires Pro)
  - Dorico SE 6.x: NO
- Glyphs: SMuFL Standard Accidentals — accidentalSharp 0xE262, accidentalFlat 0xE260, accidentalNatural 0xE261
- Font: `font.defaultmusic` (Bravura) for glyphs; `font.defaulttext` for cent labels (font-override caveat)

From STATE.md (Phase 1/2/3 outcomes):
- Total accidentals shipped: 597 (3 zero-deviation + 594 non-zero across ±99¢)
- Naming convention: `Sharp +14`, `Flat -50`, `Natural -7` (signed cents always present)
- File: `cents.doricolib` at repo root, 1,261,618 bytes, md5 `4cd707d2f4b10154a528b95e2ff5db9f`
- Determinism: `uuid.uuid5(PROJECT_NAMESPACE, key)` — re-imports update existing entries instead of duplicating (Pitfall 6)

From 03-02-SUMMARY.md (Phase 3 playback verification):
- HALion validated: 12-row tuner spot-check matrix passed at ±1¢ on Dorico Pro 6.2.2 macOS
- Off-by-100 trap diagnostic probes both passed (Sharp +50 → +150¢, Flat -7 → -107¢)
- Enharmonic-equivalent pair Sharp -50 ≡ Natural +50 audibly identical
- Sparse + dense passage layout: clean (no collisions)

From 03-01-SUMMARY.md (Phase 3 panel population):
- Panel populates with all 597 entries after Library Manager import + open key signature applied
- Panel-search ergonomics validated: `+14` → 3 matches, `Sharp -` → 99 matches, `Flat +50` → 1 match, `Natural` → 199 matches; usable interactive time
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write README.md with all ten DIST-01 sections in order</name>
  <files>README.md</files>
  <read_first>
    - ./CLAUDE.md (authoritative content for install paths, version compat, SMuFL info, pitfall list, font references)
    - .planning/ROADMAP.md (Phase 4 success criteria)
    - .planning/STATE.md (Phase 1/2/3 outcomes — 597 entry count, md5, file size)
    - .planning/phases/03-dorico-import-playback-validation/03-02-SUMMARY.md (HALion playback validation results to cite)
    - .planning/phases/03-dorico-import-playback-validation/03-01-SUMMARY.md (panel population + search results, Dorico build version 6.2.2)
    - Verify no existing README.md at repo root: `ls README.md 2>&1` should return "No such file"
  </read_first>
  <action>
Create `README.md` at the repo root containing the ten sections below in this exact order, with every required literal string present. Use Markdown headings (`#`, `##`, `###`). The README is shipped alongside `cents.doricolib` and `LICENSE`.

**IMPORTANT — heading text is load-bearing:** The exact section heading strings prescribed below are asserted by ordering checks in the verify block. Use them verbatim — do not paraphrase, reorder, or change punctuation/casing.

**Section 1 — Project name + tagline (top of file).**

Heading: `# Cents — Custom Tonality System for Dorico`

One-line description (next paragraph): `Cent-accurate microtonal accidentals for Steinberg Dorico Pro 6. Every accidental is a familiar natural / sharp / flat glyph paired with a small signed cent label (e.g., \`Sharp +14\` displays C♯ with \`+14\` above the staff and plays back 14 cents sharp of C♯).`

**Section 2 — Requirements (front-loaded; this is the very first heading after the tagline).**

Heading (verbatim): `## Requirements`

Body: `**Dorico Pro 6.0 or later.** This library will not load on Dorico 5, Dorico Elements, or Dorico SE. See the [Compatibility](#compatibility) section below for details.`

**Section 3 — Package contents.**

Heading (verbatim): `## Package Contents`

Bulleted list (literal):
- `cents.doricolib` — the tonality system library (597 accidentals, ~1.2 MB)
- `README.md` — this file
- `LICENSE` — MIT license

**Section 4 — Library Manager install (primary path).**

Heading (verbatim): `## Install (Recommended): Library Manager`

Body opener: `Library Manager is the primary install path. It scopes the library to a single project, is recoverable on failure, and was the path validated on Dorico Pro 6.2.2 during development.`

Numbered steps:
1. Open the Dorico project where you want to use cent-accurate accidentals.
2. Go to **Library → Library Manager**.
3. In the left pane, click **Import**.
4. Select `cents.doricolib` from this download.
5. Confirm import. The library is now scoped to this project only.
6. Save the project to persist the library reference.

Closing line: `If the import fails, no system files are touched — close Library Manager and try the [DefaultLibraryAdditions path](#install-power-user-defaultlibraryadditions) below, or see [Troubleshooting](#troubleshooting).`

**Section 5 — DefaultLibraryAdditions install (power-user path) with launch-crash warning.**

Heading (verbatim): `## Install (Power User): DefaultLibraryAdditions`

Opening warning paragraph (must contain the literal string `remove if Dorico fails to launch`):

`> **Warning:** Files dropped into \`DefaultLibraryAdditions/\` are loaded by Dorico at startup. If Dorico fails to launch after installing here, **remove if Dorico fails to launch** — delete \`cents.doricolib\` from the folder below and Dorico will recover on the next start. Library Manager (above) is safer because it scopes per-project.`

Then platform paths (literal, copy-paste exact — note Windows path uses `%APPDATA%` with uppercase letters between percent signs, the canonical Windows env-var form):

**macOS:**
```
~/Library/Application Support/Steinberg/Dorico 6/DefaultLibraryAdditions/
```

**Windows:**
```
%APPDATA%\Steinberg\Dorico 6\DefaultLibraryAdditions\
```

Steps (numbered):
1. Quit Dorico.
2. Drop `cents.doricolib` into the path for your OS above.
3. Re-launch Dorico. The library loads globally for every project.
4. If Dorico fails to launch, delete the file from this folder and Dorico will recover.

**Section 6 — First-note walkthrough (must include Shift+K → "open" step).**

Heading (verbatim): `## Your First Cent-Accurate Note`

Opening paragraph: `Cent-accurate accidentals will not appear in the panel until the flow is in an open (atonal) key signature. Skipping this step is the #1 silent failure for any custom tonality system in Dorico.`

Numbered steps (this is the critical walkthrough — every step is required):

1. **Set the flow to an open key signature.** With the caret active in Write mode, press **Shift+K** to open the key signatures popover, type `open`, and press Enter. (You can also use **Write → Create Key Signature → Open/Atonal**.)
2. **Apply the cents tonality system to the player.** Go to **Setup mode → Players panel**. Click the player you want to write microtonal music for. In the right-hand properties panel, set **Tonality System** to `cents`.
3. **Open the Accidentals panel.** Return to **Write mode**. Open the right-hand panel **Key Signatures, Tonality Systems, and Accidentals**. You should see all 597 accidentals listed.
4. **Search for a cent value.** Type `+14` into the panel search to filter. You should see three matches — `Sharp +14`, `Flat +14`, `Natural +14`.
5. **Apply the accidental.** With a note selected, click `Sharp +14`. The note now displays a sharp glyph with `+14` floating above the staff, and plays back 14 cents sharp of the written pitch.
6. **Verify with a tuner.** Play the note back via HALion or NotePerformer (see [Playback compatibility](#troubleshooting) below). A reference tuner should read 14 cents sharp of the natural pitch (validated to ±1¢ on Dorico Pro 6.2.2 macOS during development).

Closing line: `If the panel is empty, the open key signature step (#1) was skipped — go back and apply Shift+K → "open".`

**Section 7 — Naming convention reference.**

Heading (verbatim): `## Naming Convention`

Opening paragraph: `Every accidental name follows the pattern \`<Base> <signed-cents>\`, where the base is one of \`Sharp\`, \`Flat\`, or \`Natural\` and the cents value is signed (always with \`+\` or \`-\`).`

Examples table:

| Name | Glyph | Cents from natural | Notes |
|------|-------|--------------------|-------|
| `Sharp` | ♯ | +100¢ | zero-deviation; standard sharp |
| `Flat` | ♭ | -100¢ | zero-deviation; standard flat |
| `Natural` | ♮ | 0¢ | zero-deviation; standard natural |
| `Sharp +14` | ♯ + `+14` | +114¢ | sharp 14¢ further sharp |
| `Flat -50` | ♭ + `-50` | -150¢ | flat 50¢ further flat |
| `Natural -7` | `-7` only | -7¢ | natural-base shows cents only |
| `Sharp -50` | ♯ + `-50` | +50¢ | enharmonic equivalent of `Natural +50` |

Closing paragraph: `Search ergonomics validated at the 597-entry scale: \`+14\` returns 3 matches, \`Sharp -\` returns 99 matches, \`Flat +50\` returns 1 match, \`Natural\` returns 199 matches.`

**Section 8 — Troubleshooting (priority order: open-key-sig FIRST, then VST, then font).**

Heading (verbatim): `## Troubleshooting`

**Subsection 8a (must be first — heading verbatim):**

Heading: `### The Accidentals panel is empty / the cents accidentals don't appear`

Body: `This is the #1 silent failure. The cents tonality system only displays accidentals when the flow is in an **open (atonal) key signature**. To fix:`

Numbered fix:
1. In Write mode, with the caret active, press **Shift+K**.
2. Type `open` and press Enter.
3. The key signature is now open/atonal, and the 597 accidentals will appear in the panel.

Then: `If the panel still shows the standard 12-EDO accidentals only, confirm the player has \`cents\` set as its Tonality System in Setup mode (see [Your First Cent-Accurate Note](#your-first-cent-accurate-note) step 2).`

**Subsection 8b (heading verbatim):**

Heading: `### Playback is in 12-EDO instead of cent-accurate`

Body: `Dorico itself plays back at the labeled cent value (validated to ±1¢ in HALion on Dorico Pro 6.2.2). If you hear 12-EDO playback, the cause is almost always the playback instrument:`

| Instrument | Microtonal playback | Notes |
|------------|---------------------|-------|
| **HALion** (Dorico's bundled sampler) | Yes — confirmed in Phase 3 validation | Default; recommended |
| **NotePerformer** | Yes — supports per-note pitch bends | Confirmed compatible |
| **Kontakt** | Caveat | Requires per-instrument microtonal support; Kontakt's host-pitch-bend MIDI semantics vary by library. Test before relying on. |
| **SWAM** instruments | Caveat | Some SWAM instruments respond to per-note pitch deviations, others quantize to 12-EDO. Test before relying on. |
| **Falcon** (UVI) | Caveat | Microtonal support is patch-dependent. Test before relying on. |

Then: `If your VST quantizes to 12-EDO regardless of Dorico's pitch instruction, that is a VST limit, not a library limit. Switch to HALion or NotePerformer for verified cent-accurate output.`

**Subsection 8c (heading verbatim):**

Heading: `### Cent labels render in the wrong font / look weird`

Body: `Cent labels use the \`font.defaulttext\` font style, which inherits from your project's default text font. If you have overridden \`font.defaulttext\` to a non-monospaced or unusual font, cent labels (e.g., \`+14\`) may render with poor kerning or unexpected weight. To fix, either revert the project default text font or override \`font.defaulttext\` to a clean sans-serif (Academico Bold or the Dorico default).`

**Subsection 8d:**

Heading: `### Re-importing the library duplicated entries / I want a clean install`

Body: `Re-imports do not duplicate. Every accidental's \`entityID\` is derived deterministically (\`uuid5(PROJECT_NAMESPACE, key)\`), so a re-import of the same \`cents.doricolib\` updates existing entries in place rather than appending duplicates. If you want a clean state, remove the library via Library Manager and re-import.`

**Section 9 — Compatibility.**

Heading (verbatim): `## Compatibility`

| Dorico Edition | Will the file load? | Notes |
|----------------|---------------------|-------|
| Dorico Pro 6.0–6.2.x | **Yes** (target) | Validated on Dorico Pro 6.2.2 macOS |
| Dorico Pro 5.x | **No** | Library format changed between Dorico 5 and 6 |
| Dorico Pro 4.x and below | **No** | Older library format |
| Dorico Elements 6.x | **Partial** | Library import may succeed, but tonality-system editing requires Pro |
| Dorico SE 6.x | **No** | SE lacks Library Manager and tonality-system support |

Closing line: `This library is built against \`fileVersion 1.1450\` (Dorico Pro 6.x). It will not be back-ported to Dorico 5.`

**Section 10 — License.**

Heading (verbatim): `## License`

Body: `MIT License. See [LICENSE](LICENSE) for the full text.`

Copyright line: `Copyright (c) 2026 Taylor Brook.`

**Final verification before write:** Re-read your output and confirm every literal string from `<acceptance_criteria>` below is present, character-exact (including `Shift+K`, `open`, the macOS path with `~/Library/Application Support/Steinberg/Dorico 6/DefaultLibraryAdditions/` literal, the Windows path with `%APPDATA%`, `remove if Dorico fails to launch`, `font.defaulttext`, `597`, all four naming examples, HALion + NotePerformer + Kontakt, `Dorico Elements`, `Dorico SE`, `fileVersion 1.1450`). Then write to `README.md` at the repo root.
  </action>
  <acceptance_criteria>
    - `README.md` exists at repo root: `test -f README.md`
    - First H1 is the project title: `head -1 README.md | grep -F '# Cents — Custom Tonality System for Dorico'`
    - Contains `## Requirements` heading: `grep -F '## Requirements' README.md`
    - Lists Dorico Pro 6.0+ requirement: `grep -F 'Dorico Pro 6.0' README.md`
    - References Library Manager: `grep -F 'Library Manager' README.md`
    - Contains the literal macOS path: `grep -F '~/Library/Application Support/Steinberg/Dorico 6/DefaultLibraryAdditions/' README.md`
    - Contains the literal Windows path with `%APPDATA%` (canonical env-var form, uppercase): `grep -F '%APPDATA%' README.md`
    - Contains the launch-crash warning verbatim: `grep -i 'remove if Dorico fails to launch' README.md`
    - First-note walkthrough mentions Shift+K and "open": `grep -F 'Shift+K' README.md && grep -F 'open' README.md`
    - All four naming examples present: `grep -F 'Sharp +14' README.md && grep -F 'Flat -50' README.md && grep -F 'Natural -7' README.md && grep -F 'Sharp -50' README.md`
    - Troubleshooting mentions HALion, NotePerformer, and one of {Kontakt, SWAM, Falcon}: `grep -F 'HALion' README.md && grep -F 'NotePerformer' README.md && (grep -F 'Kontakt' README.md || grep -F 'SWAM' README.md || grep -F 'Falcon' README.md)`
    - Font caveat present: `grep -F 'font.defaulttext' README.md`
    - 597 entry count cited: `grep -F '597' README.md`
    - References LICENSE file: `grep -F 'LICENSE' README.md`
    - Contains a Compatibility section: `grep -F '## Compatibility' README.md`
    - Compatibility matrix names Dorico Elements: `grep -F 'Dorico Elements' README.md`
    - Compatibility matrix names Dorico SE: `grep -F 'Dorico SE' README.md`
    - Compatibility section cites the schema constant: `grep -F 'fileVersion 1.1450' README.md`
    - Cites Dorico Pro 6.2.2 (validated build): `grep -F '6.2.2' README.md`
    - File length sanity (>= 100 lines): `[ $(wc -l < README.md) -ge 100 ]`
    - **Ordering: Requirements precedes Package Contents** — `awk '/^## Requirements/{r=NR} /^## Package Contents/{p=NR} END{exit !(r && p && r<p)}' README.md`
    - **Ordering: Library Manager install precedes DefaultLibraryAdditions install** — `awk '/^## Install \(Recommended\): Library Manager/{lm=NR} /^## Install \(Power User\): DefaultLibraryAdditions/{dla=NR} END{exit !(lm && dla && lm<dla)}' README.md`
    - **Ordering: open-key-signature troubleshooting subsection precedes the playback/VST subsection** — `awk '/^### The Accidentals panel is empty/{ok=NR} /^### Playback is in 12-EDO/{vst=NR} END{exit !(ok && vst && ok<vst)}' README.md`
    - **Ordering: playback/VST subsection precedes the font subsection** — `awk '/^### Playback is in 12-EDO/{vst=NR} /^### Cent labels render in the wrong font/{font=NR} END{exit !(vst && font && vst<font)}' README.md`
  </acceptance_criteria>
  <verify>
    <automated>test -f README.md && head -1 README.md | grep -qF '# Cents — Custom Tonality System for Dorico' && grep -qF '## Requirements' README.md && grep -qF 'Dorico Pro 6.0' README.md && grep -qF 'Library Manager' README.md && grep -qF '~/Library/Application Support/Steinberg/Dorico 6/DefaultLibraryAdditions/' README.md && grep -qF '%APPDATA%' README.md && grep -qiF 'remove if Dorico fails to launch' README.md && grep -qF 'Shift+K' README.md && grep -qF 'Sharp +14' README.md && grep -qF 'Flat -50' README.md && grep -qF 'Natural -7' README.md && grep -qF 'Sharp -50' README.md && grep -qF 'HALion' README.md && grep -qF 'NotePerformer' README.md && (grep -qF 'Kontakt' README.md || grep -qF 'SWAM' README.md || grep -qF 'Falcon' README.md) && grep -qF 'font.defaulttext' README.md && grep -qF '597' README.md && grep -qF 'LICENSE' README.md && grep -qF '## Compatibility' README.md && grep -qF 'Dorico Elements' README.md && grep -qF 'Dorico SE' README.md && grep -qF 'fileVersion 1.1450' README.md && grep -qF '6.2.2' README.md && [ $(wc -l < README.md) -ge 100 ] && awk '/^## Requirements/{r=NR} /^## Package Contents/{p=NR} END{exit !(r && p && r<p)}' README.md && awk '/^## Install \(Recommended\): Library Manager/{lm=NR} /^## Install \(Power User\): DefaultLibraryAdditions/{dla=NR} END{exit !(lm && dla && lm<dla)}' README.md && awk '/^### The Accidentals panel is empty/{ok=NR} /^### Playback is in 12-EDO/{vst=NR} END{exit !(ok && vst && ok<vst)}' README.md && awk '/^### Playback is in 12-EDO/{vst=NR} /^### Cent labels render in the wrong font/{font=NR} END{exit !(vst && font && vst<font)}' README.md && echo "README ACCEPTANCE PASSED"</automated>
  </verify>
  <done>
    `README.md` exists at repo root containing all ten DIST-01 sections in order. All literal strings enumerated in `<acceptance_criteria>` are present, all ordering checks pass, and the file is at least 100 lines. The verify command prints `README ACCEPTANCE PASSED`.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| README → user filesystem | User reads README and physically places `cents.doricolib` into a system folder; instructions must not strand the user with a broken Dorico install. |
| README → user expectations | User reads playback claims; mismatch between claim and observed behavior creates blame on the library when the actual cause is environment (VST, key signature, Dorico edition). |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-01 | Denial of Service (user-experienced) | DefaultLibraryAdditions install instructions | mitigate | README must include the literal warning `remove if Dorico fails to launch` adjacent to the path, with a concrete recovery step (delete the file, Dorico recovers on next launch). Library Manager is presented as the primary path precisely because it is non-destructive on failure. |
| T-04-02 | Repudiation (user blames library for environment failure) | Version compatibility section | mitigate | Front-load the Dorico Pro 6.0+ requirement as the first heading after the tagline, and ship a full compatibility matrix in the Compatibility section that explicitly says Dorico 5 / Elements / SE will not load this library. A user on an unsupported edition cannot claim they were not warned. |
| T-04-03 | Information Disclosure (false expectation) | Troubleshooting / playback section | mitigate | Distinguish library-side guarantees (Dorico's pitch instruction is cent-accurate, validated to ±1¢ on HALion) from environment-side limits (third-party VSTs may quantize). The troubleshooting table names HALion + NotePerformer as confirmed and Kontakt + SWAM + Falcon as caveat — so a user hearing 12-EDO playback in Kontakt knows to switch instruments rather than blame the library. The open-key-signature gotcha leads troubleshooting because it is the single most common silent-failure cause. |
</threat_model>

<verification>
Phase verification across all three plans:
- DIST-01 closed by Plan 04-01 (this plan).
- DIST-02 closed by Plan 04-02 (LICENSE).
- DIST-03 closed by Plan 04-03 (manual user install verification).

This plan's specific verification: the Task 1 acceptance criteria grep + awk ordering checks all pass, and the README is human-readable end-to-end (executor should `cat README.md` once after writing to confirm visual flow).
</verification>

<success_criteria>
- README.md exists at repo root, autonomous-written by the executor.
- All ten DIST-01 sections present in order.
- All literal-string acceptance criteria pass under `grep`, all section-ordering acceptance criteria pass under `awk`.
- README is shipped alongside `cents.doricolib` (already at repo root) and `LICENSE` (created in parallel by Plan 04-02).
- A user reading top-to-bottom can install on Dorico Pro 6.x, set up an open key signature, write `Sharp +14`, and verify via tuner — without leaving the README.
</success_criteria>

<output>
After completion, create `.planning/phases/04-readme-packaging/04-01-SUMMARY.md` capturing:
- DIST-01 closure status
- README.md final line count
- Section-by-section content audit (all ten sections present, in order)
- Any deviations from the plan text (e.g., wording choices the executor made for readability)
</output>
</content>
</invoke>