---
phase: 04-readme-packaging
plan: 03
type: execute
wave: 2
depends_on:
  - 04-01
  - 04-02
files_modified:
  - .planning/phases/04-readme-packaging/04-VERIFICATION-NOTES.md
  - README.md
autonomous: false
requirements:
  - DIST-03
tags:
  - manual-validation
  - distribution
  - dorico-pro-6
  - install-verification
must_haves:
  truths:
    - "The user has physically followed the README install instructions on their actual macOS Dorico Pro 6.x install"
    - "The Library Manager install path documented in the README works end-to-end on the target machine"
    - "Any divergence between README instructions and actual UI behavior is documented and either patched in the README or accepted as a Dorico-version difference"
    - "The 597-accidental panel populates after the README's first-note walkthrough is followed verbatim"
  artifacts:
    - path: ".planning/phases/04-readme-packaging/04-VERIFICATION-NOTES.md"
      provides: "User's report from physically running the README install + first-note walkthrough"
      min_lines: 20
  key_links:
    - from: "04-VERIFICATION-NOTES.md"
      to: "README.md"
      via: "Notes either confirm README is accurate or trigger a README patch"
      pattern: "README"
---

<objective>
Verify end-to-end that the README's install instructions and first-note walkthrough work on the user's actual macOS Dorico Pro 6.x install. This is the only DIST-03 closure path: only the user can physically perform the install. The plan provides the verification protocol; the user executes it; the executor records the result and patches the README if any step's wording diverges from observed UI.

This plan is **autonomous: false** — Task 1 is a `checkpoint:human-action` because Library Manager import is a UI action (Dorico has no headless mode; per CLAUDE.md, manual playback / UI verification is the only validation path). Task 2 is the executor's response to the user's report.

Purpose: Close DIST-03 ("the .doricolib and README install path is verified on the user's actual macOS Dorico install"). Closes the phase.

Output: `.planning/phases/04-readme-packaging/04-VERIFICATION-NOTES.md` documenting what the user did, what they observed, and any README patches applied.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/04-readme-packaging/04-01-PLAN.md
@.planning/phases/04-readme-packaging/04-02-PLAN.md
@.planning/phases/03-dorico-import-playback-validation/03-01-SUMMARY.md
@./README.md
@./LICENSE

<interfaces>
<!-- Required state before this plan runs -->
- README.md exists at repo root (created by Plan 04-01).
- LICENSE exists at repo root (created by Plan 04-02).
- cents.doricolib exists at repo root (created in Phase 2; md5 4cd707d2f4b10154a528b95e2ff5db9f).
- The user has Dorico Pro 6.x installed on macOS (build 6.2.2 confirmed in Phase 3 03-01-SUMMARY).
</interfaces>
</context>

<tasks>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 1: User runs README install + first-note walkthrough on their machine</name>
  <files>(none — user-side action)</files>
  <read_first>
    - README.md (the user follows the README's instructions verbatim — this is the test)
  </read_first>
  <what-built>
    Plan 04-01 wrote `README.md`. Plan 04-02 wrote `LICENSE`. Phase 2 produced `cents.doricolib` at the repo root. The complete shipped artifact set now exists at the repo root.
  </what-built>
  <how-to-verify>
    The user (only the user can do this — Dorico has no headless mode) physically performs the following on their macOS Dorico Pro 6.x install. The user follows **only what is in README.md** — do not consult this plan or any other doc. If a README step is ambiguous or the UI does not match, that is a finding to report.

    **Pre-check (informational, before starting):**
    1. Confirm `README.md`, `LICENSE`, and `cents.doricolib` all exist at the repo root.
    2. Confirm the running Dorico version is Pro 6.0 or later. (Phase 3 used 6.2.2; any 6.x build is valid.)

    **Test path A — Library Manager (primary path the README recommends):**

    Open the README in a viewer and follow the **Install (Recommended): Library Manager** section verbatim. Then follow **Your First Cent-Accurate Note** verbatim, in a fresh test project.

    Record for each numbered step in the README's first-note walkthrough whether the UI matches what the README describes:

    | Step | README says | Observed | OK? |
    |------|-------------|----------|-----|
    | 1 | Shift+K → type `open` → Enter | (record what the user saw) | yes/no |
    | 2 | Setup mode → Players → Tonality System → `cents` | (record) | yes/no |
    | 3 | Write mode → open the Accidentals panel → 597 entries visible | (record) | yes/no |
    | 4 | Search `+14` → 3 matches | (record actual count) | yes/no |
    | 5 | Apply `Sharp +14` → glyph + `+14` label appears | (record) | yes/no |
    | 6 | Tuner reads ~14¢ sharp (HALion or NotePerformer) | (record actual reading) | yes/no |

    **Test path B — DefaultLibraryAdditions (power-user path) — OPTIONAL but recommended:**

    Quit Dorico. Drop a copy of `cents.doricolib` into `~/Library/Application Support/Steinberg/Dorico 6/DefaultLibraryAdditions/`. Re-launch Dorico.

    - Confirm Dorico launches successfully (does not crash).
    - Confirm `cents` is available as a tonality system in a fresh project without manual import.
    - If Dorico fails to launch: delete the file from that folder per the README warning, confirm Dorico recovers on next start, and report this as a finding (the README's recovery instruction must work).

    **Resume signal:** Reply with one of:

    - `approved` — both test paths (or Path A alone, if Path B was skipped) succeeded; README is accurate; no patches needed.
    - `approved with notes: <list>` — succeeded but with minor wording differences worth patching (e.g., "the menu is actually called 'Library Manager…' with an ellipsis"). List each note.
    - `failed: <description>` — a step did not work; describe which step, what was observed, and what the user thinks the correct README wording or sequence should be.
  </how-to-verify>
  <acceptance_criteria>
    - User has physically opened Dorico Pro 6.x on macOS.
    - User has run Library Manager → Import → cents.doricolib at least once on a real project.
    - User has applied Shift+K → "open" key signature in that project.
    - User has set the player's Tonality System to `cents` in Setup mode.
    - User has confirmed the Accidentals panel populates with 597 entries.
    - User has applied at least one cent-accurate accidental (e.g., `Sharp +14`) and heard playback.
    - User has reported back with `approved`, `approved with notes`, or `failed: <description>`.
  </acceptance_criteria>
  <resume-signal>Type one of: `approved`, `approved with notes: <list>`, or `failed: <description>`.</resume-signal>
  <done>
    User has provided a structured response (one of the three forms above), and the executor has captured it for use in Task 2.
  </done>
</task>

<task type="auto">
  <name>Task 2: Record user verification result and patch README if needed</name>
  <files>
    .planning/phases/04-readme-packaging/04-VERIFICATION-NOTES.md
    README.md (only if user reported a wording divergence requiring a patch)
  </files>
  <read_first>
    - The user's response from Task 1 (in conversation context).
    - README.md (if a patch is needed, to know current wording before editing).
    - .planning/phases/03-dorico-import-playback-validation/03-VERIFICATION.md (existing convention for how phase verification files are structured — for stylistic consistency).
  </read_first>
  <action>
Based on the user's Task 1 resume signal, take exactly one of these three branches:

**Branch A — User said `approved`:**

Write `.planning/phases/04-readme-packaging/04-VERIFICATION-NOTES.md` with this structure:

```markdown
# Phase 04 — DIST-03 Manual Install Verification

**Date:** <today's date in YYYY-MM-DD>
**Dorico build:** <ask the user or default to "Dorico Pro 6.x macOS — exact build not recorded">
**Result:** PASS — all README steps validated end-to-end on user's machine.

## Test Path A — Library Manager (primary path)

| Step | README says | Observed | OK? |
|------|-------------|----------|-----|
| 1 | Shift+K → type `open` → Enter | matched | yes |
| 2 | Setup mode → Players → Tonality System → `cents` | matched | yes |
| 3 | Accidentals panel populates with 597 entries | matched | yes |
| 4 | Search `+14` → 3 matches | matched | yes |
| 5 | Apply `Sharp +14` — glyph + `+14` label appears | matched | yes |
| 6 | Tuner reads ~14¢ sharp | matched | yes |

## Test Path B — DefaultLibraryAdditions (power-user path)

<If Path B was tested: matching table. If skipped: write "Not tested in this verification round; covered by README warning + Phase 3 03-01-SUMMARY (Library Manager path used to avoid Pitfall 4 risk).">

## DIST-03 Closure

User-confirmed end-to-end install + first-note flow on their actual macOS Dorico Pro 6.x install. README is accurate. No patches applied.
```

Then run the README acceptance criteria from Plan 04-01 one more time as a regression sanity check (the verify command in 04-01 Task 1).

**Branch B — User said `approved with notes: <list>`:**

For each note, decide if it's a wording patch (apply now) or a non-blocking observation (record only).

- Wording patches: edit `README.md` minimally — change only the words the user flagged, keep all other content unchanged. Re-run the Plan 04-01 acceptance verify command afterward to confirm no required strings were lost.
- Observations: record in the verification notes file but do not patch.

Write `.planning/phases/04-readme-packaging/04-VERIFICATION-NOTES.md` with the same structure as Branch A but with the result line `PASS WITH NOTES`, a per-note section listing each user note + disposition (patched or observation), and a `## README Patches Applied` section listing the exact diffs (file + before/after lines).

**Branch C — User said `failed: <description>`:**

The verification did not pass. Diagnose the failure with the user (in chat) — was it a README wording bug, a Dorico-version difference, a user environment problem, or a real library bug?

- README wording bug → patch the README, ask user to re-run the failing step, repeat until pass, then proceed as Branch A.
- Dorico-version difference → add a `### Dorico version notes` callout to the README's Compatibility section documenting the divergence; ask user to confirm the new wording resolves it.
- User environment problem (e.g., wrong Dorico edition) → no README change needed; document in the verification notes.
- Library bug → STOP. Do not write a PASS verification. Surface the bug to the user with a recommendation: re-open Phase 3 (if it's a playback or panel issue) or re-open Phase 2 (if it's an entity-count or naming issue). DIST-03 stays unclosed until the bug is resolved.

After resolution, write `.planning/phases/04-readme-packaging/04-VERIFICATION-NOTES.md` with result `PASS AFTER FIX` (or `BLOCKED — see X` if the library bug path was hit), the failing-step description, the resolution applied, and (if applicable) a `## README Patches Applied` section.

**In every branch:** the verification notes file MUST exist before this task is considered done. The README patch (if any) MUST not strip any of the literal strings or violate any of the section-ordering checks required by Plan 04-01's acceptance criteria — re-run that verify command after any edit.

**Source-of-truth note for the executor:** the `<automated>` block below inlines the FULL Plan 04-01 acceptance verify chain (literal strings + ordering awk checks + line-count gate). It is gated on `test -f README.md` as a hard precondition (this plan depends on 04-01, so README must exist). If Plan 04-01's `<automated>` chain is ever changed, this Task 2 `<automated>` block MUST be updated to match — keep them in sync.
  </action>
  <acceptance_criteria>
    - `.planning/phases/04-readme-packaging/04-VERIFICATION-NOTES.md` exists: `test -f .planning/phases/04-readme-packaging/04-VERIFICATION-NOTES.md`
    - File names a result: `grep -E '^\*\*Result:\*\*\s+(PASS|PASS WITH NOTES|PASS AFTER FIX|BLOCKED)' .planning/phases/04-readme-packaging/04-VERIFICATION-NOTES.md`
    - File documents at least the Library Manager test path (Path A): `grep -F 'Library Manager' .planning/phases/04-readme-packaging/04-VERIFICATION-NOTES.md`
    - File mentions the 597 entry count check: `grep -F '597' .planning/phases/04-readme-packaging/04-VERIFICATION-NOTES.md`
    - File length sanity (>= 20 lines): `[ $(wc -l < .planning/phases/04-readme-packaging/04-VERIFICATION-NOTES.md) -ge 20 ]`
    - README.md still exists (hard precondition — this plan depends on 04-01): `test -f README.md`
    - README.md still passes Plan 04-01's full acceptance chain (literal strings + ordering awk checks + line-count gate). No required strings stripped, no section-ordering invariants broken by any patch.
    - If result is BLOCKED, no SUMMARY-level closure of DIST-03 is claimed; the phase remains open with a blocker note in STATE.md.
  </acceptance_criteria>
  <verify>
    <automated>test -f .planning/phases/04-readme-packaging/04-VERIFICATION-NOTES.md && grep -qE '^\*\*Result:\*\*\s+(PASS|PASS WITH NOTES|PASS AFTER FIX|BLOCKED)' .planning/phases/04-readme-packaging/04-VERIFICATION-NOTES.md && grep -qF 'Library Manager' .planning/phases/04-readme-packaging/04-VERIFICATION-NOTES.md && grep -qF '597' .planning/phases/04-readme-packaging/04-VERIFICATION-NOTES.md && [ $(wc -l < .planning/phases/04-readme-packaging/04-VERIFICATION-NOTES.md) -ge 20 ] && test -f README.md && head -1 README.md | grep -qF '# Cents — Custom Tonality System for Dorico' && grep -qF '## Requirements' README.md && grep -qF 'Dorico Pro 6.0' README.md && grep -qF 'Library Manager' README.md && grep -qF '~/Library/Application Support/Steinberg/Dorico 6/DefaultLibraryAdditions/' README.md && grep -qF '%APPDATA%' README.md && grep -qiF 'remove if Dorico fails to launch' README.md && grep -qF 'Shift+K' README.md && grep -qF 'Sharp +14' README.md && grep -qF 'Flat -50' README.md && grep -qF 'Natural -7' README.md && grep -qF 'Sharp -50' README.md && grep -qF 'HALion' README.md && grep -qF 'NotePerformer' README.md && (grep -qF 'Kontakt' README.md || grep -qF 'SWAM' README.md || grep -qF 'Falcon' README.md) && grep -qF 'font.defaulttext' README.md && grep -qF '597' README.md && grep -qF 'LICENSE' README.md && grep -qF '## Compatibility' README.md && grep -qF 'Dorico Elements' README.md && grep -qF 'Dorico SE' README.md && grep -qF 'fileVersion 1.1450' README.md && grep -qF '6.2.2' README.md && [ $(wc -l < README.md) -ge 100 ] && awk '/^## Requirements/{r=NR} /^## Package Contents/{p=NR} END{exit !(r && p && r<p)}' README.md && awk '/^## Install \(Recommended\): Library Manager/{lm=NR} /^## Install \(Power User\): DefaultLibraryAdditions/{dla=NR} END{exit !(lm && dla && lm<dla)}' README.md && awk '/^### The Accidentals panel is empty/{ok=NR} /^### Playback is in 12-EDO/{vst=NR} END{exit !(ok && vst && ok<vst)}' README.md && awk '/^### Playback is in 12-EDO/{vst=NR} /^### Cent labels render in the wrong font/{font=NR} END{exit !(vst && font && vst<font)}' README.md && echo "DIST-03 VERIFICATION RECORDED"</automated>
  </verify>
  <done>
    Verification notes file exists, names a result, and either (a) confirms PASS without README patches, (b) confirms PASS WITH NOTES with the README patches applied and Plan 04-01 acceptance still passing, (c) confirms PASS AFTER FIX with the failing step's resolution documented, or (d) is marked BLOCKED with a clear pointer to which earlier phase needs re-opening. README still passes Plan 04-01's full acceptance chain (literal strings + ordering checks). The verify command prints `DIST-03 VERIFICATION RECORDED`.
  </done>
</task>

</tasks>

<threat_model>
Threat surface for this phase is covered in 04-01's threat model. This plan's specific threat is **the README documents a UI flow that doesn't actually work on the user's Dorico build** — mitigated by Task 1 (user physically runs the flow) and Task 2 Branch B/C (executor patches the README if divergence is found). The Path B (DefaultLibraryAdditions) test exercises the launch-crash recovery path documented in 04-01's T-04-01 mitigation.
</threat_model>

<verification>
Phase verification is the conjunction of all three plans:
- Plan 04-01 closes DIST-01 (README content).
- Plan 04-02 closes DIST-02 (LICENSE content).
- Plan 04-03 (this plan) closes DIST-03 (user-physical install verification + any README patches needed for accuracy).

This plan's verification: Task 2's grep + awk checks pass, the verification notes name a result, and (if a non-BLOCKED result) Plan 04-01's full acceptance chain still passes (no required strings stripped, no section-ordering invariants broken by patches).
</verification>

<success_criteria>
- The user has physically run the README install + first-note walkthrough on their actual macOS Dorico Pro 6.x install.
- 04-VERIFICATION-NOTES.md records the result.
- Any README wording bug found during verification is patched in README.md before phase closure.
- DIST-03 is closed (PASS, PASS WITH NOTES, or PASS AFTER FIX) — or the phase is explicitly BLOCKED with a documented reason.
</success_criteria>

<output>
After completion, create `.planning/phases/04-readme-packaging/04-03-SUMMARY.md` capturing:
- DIST-03 closure status (or BLOCKED with reason)
- Test paths exercised (A only, or A + B)
- Any README patches applied (file diff summary)
- Final phase-closure recommendation: ready for /gsd-verify-phase + /gsd-transition, or stays open
</output>
</content>
</invoke>